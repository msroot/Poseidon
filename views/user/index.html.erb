<h2>All Users</h2>

<ul><% @users.each do |user| %>
	<li>
		<h4><%= user.name %></h4>

		<ul>
			<% user.posts.each do |post| %>
			<li><%= link_to post.title, post %> </li>
			<% end %>
		</ul>
	</li>
	<% end %>
</ul>

<%= link_to "Add new User", @users.last, "new" %>
